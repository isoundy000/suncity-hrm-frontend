import React, { Component } from 'react';import style from '../../jobApplication.less';const LogTitle = ({type, operator, created_at})=> {  function Content(type, operator, created_at) {    if (type == "agreed_interview") {      return (        <div>          <li className={style.dotBlue}>            <span className={style.log}>{operator.chinese_name}更新求職狀態為 同意面試              <span className={style.logTime}>{created_at}</span>            </span>          </li>        </div>      )    }    if (type == "update_interview") {      return (        <div>          <li className={style.dotYellow}>            <span className={style.log}>{operator.chinese_name}修改了面試              <span className={style.logTime}>{created_at}</span>            </span>          </li>        </div>      )    }  }  return (    <div>      {        Content(type, operator, created_at)      }    </div>  );};export default LogTitle;